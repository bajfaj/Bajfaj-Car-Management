{
  "name": "bajfaj-car-management",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack web application to manage car inventory, replacing manual spreadsheet workflows.",
  "scripts": {
    "dev": "concurrently \"npm:dev:client\" \"npm:dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}